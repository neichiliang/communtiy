<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.myheart.community.mapper.CommentExtMapper">
    <resultMap id="BaseResultMap" type="life.myheart.community.model.Comment">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Jun 04 08:06:47 CST 2019.
        -->
        <id column="ID" jdbcType="BIGINT" property="id"/>
        <result column="PARENT_ID" jdbcType="BIGINT" property="parentId"/>
        <result column="TYPE" jdbcType="INTEGER" property="type"/>
        <result column="COMMENTATOR" jdbcType="BIGINT" property="commentator"/>
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified"/>
        <result column="LIKE_COUNT" jdbcType="BIGINT" property="likeCount"/>
        <result column="CONTENT" jdbcType="VARCHAR" property="content"/>
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount"/>
    </resultMap>
    <update id="incCommentCount" parameterType="life.myheart.community.model.Comment">
    update comment
    set
    COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where id = #{id}
    </update>

    <select id="selectSearch" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT comment.*,user.name from comment LEFT JOIN user on comment.commentator = user.id
        <where>
            <if test="id != null and id != ''">
                and comment.id like '%${id}%'
            </if>
            <if test="creator != null and creator != ''">
                and user.name like '%${creator}%'
            </if>
            <if test="content != null and content != ''">
                and comment.content like '%${content}%'
            </if>
            <if test="sensitiveword != null and sensitiveword != ''">
                and comment.state_content like '%${sensitiveword}%'
            </if>
            <if test="1==1">
                and status = 1
            </if>
        </where>
        limit #{offset},#{limit}
    </select>

    <select id="selectcomsenRelated" parameterType="java.lang.String" resultType="java.lang.Long">
        SELECT count(*) from comment LEFT JOIN user on comment.commentator = user.id
        <where>
            <if test="id != null and id != ''">
                and comment.id like '%${id}%'
            </if>
            <if test="creator != null and creator != ''">
                and user.name like '%${creator}%'
            </if>
            <if test="content != null and content != ''">
                and comment.content like '%${content}%'
            </if>
            <if test="sensitiveword != null and sensitiveword != ''">
                and comment.state_content like '%${sensitiveword}%'
            </if>
            <if test="1==1">
                and status = 1
            </if>
        </where>
    </select>
</mapper>